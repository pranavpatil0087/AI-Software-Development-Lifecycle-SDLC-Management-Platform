spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:sdlc_platform}
    username: ${DB_USERNAME:sdlc_user}
    password: ${DB_PASSWORD:sdlc_password}
  jpa:
    show-sql: true

logging:
  level:
    com.sdlcplatform: DEBUG
    org.hibernate.SQL: DEBUG
